# ── MediPredict frontend (Vite → Vercel) ──
# Copy to .env locally. On Vercel, set the production value per environment.
# No trailing slash.

# Local dev backend
VITE_API_URL=http://localhost:5000/api/v1
# Production backend (Render)
# VITE_API_URL=https://medipredict-backend.onrender.com/api/v1
