services:
  mongo:
    image: mongo:7
    ports: ["27017:27017"]
    volumes: ["mongo-data:/data/db"]
  backend:
    build: ./Backend
    ports: ["5000:5000"]
    env_file: ./Backend/.env
    depends_on: [mongo, ml]
  ml:
    build: ./ml-service
    ports: ["8000:8000"]
volumes:
  mongo-data:
